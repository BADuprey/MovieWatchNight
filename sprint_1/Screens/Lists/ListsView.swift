import SwiftUI

struct ListsView: View {
    var body: some View {
        NavigationStack {
            Text("Your lists (placeholder)")
                .padding()
                .navigationTitle("Lists")
        }
    }
}

#Preview {
    ListsView()
}
