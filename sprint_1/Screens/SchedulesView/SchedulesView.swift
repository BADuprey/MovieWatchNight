//View for schedules
import SwiftUI

struct SchedulesView: View {
    var body: some View {
        NavigationStack {
            Text("Merged schedules (placeholder)")
                .padding()
                .navigationTitle("Schedules")
        }
    }
}

#Preview {
    SchedulesView()
}
